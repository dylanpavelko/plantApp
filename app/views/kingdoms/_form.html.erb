<%= form_with(model: kingdom, local: true) do |form| %>
  <% if kingdom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kingdom.errors.count, "error") %> prohibited this kingdom from being saved:</h2>

      <ul>
      <% kingdom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
