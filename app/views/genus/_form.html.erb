<%= form_with(model: genu, local: true) do |form| %>
  <% if genu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genu.errors.count, "error") %> prohibited this genu from being saved:</h2>

      <ul>
      <% genu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :family_id %>
    <%= form.collection_select :family_id, Family.all, :id, :name, :include_blank => true %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
