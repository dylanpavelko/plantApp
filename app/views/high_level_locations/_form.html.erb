<%= form_with(model: high_level_location, local: true) do |form| %>
  <% if high_level_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(high_level_location.errors.count, "error") %> prohibited this high_level_location from being saved:</h2>

      <ul>
      <% high_level_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.text_field :zip %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
